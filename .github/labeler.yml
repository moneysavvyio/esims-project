# Add 'ROUTER' label to any change within the esimsrouter/** files within source dir
ROUTER:
  - esimsrouter/*
  - esimsrouter/**/*

# Add 'TOKEN' label to any change within the refreshdropbox/** files within source dir
TOKEN:
  - refreshdropbox/*
  - refreshdropbox/**/*

# Add 'LIB' label to any change within the lib/** files within source dir
LIB:
  - lib/*
  - lib/**/*

# Add 'INGEST' label to any change within the ingestesims/** files within source dir
INGEST:
  - ingestesims/*
  - ingestesims/**/*

# Add 'DEDUPLICATE' label to any change within the deduplicate/** files within source dir
DEDUPLICATE:
  - deduplicate/*
  - deduplicate/**/*
